const colors = {
  background: "#121A34",
  element: "#1B223D",
  border: "#202944",
  text: "#fff",
};

const dimensions = {
  header: { height: "100px" },
  border: `1px solid ${colors.border}`,
};

const theme = {
  colors,
  dimensions,
};

export default theme;
